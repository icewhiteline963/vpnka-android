<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingLeanbackLauncher">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- https://developer.android.com/about/versions/11/privacy/package-visibility -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="PackageVisibilityPolicy,QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Voice messages + WebRTC voice calls in the SmartDesk messenger. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Saving downloaded YouTube videos to public Downloads on pre-Android-10.
         On 10+ MediaStore handles it without any permission. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- Lets the in-app updater hand a downloaded APK to Android's installer
         instead of dumping the user in a browser. The system still shows its
         own confirmation and still refuses anything signed with a different
         key, so this grants us the ability to *offer*, never to install. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Lets us show the one-tap "ignore battery optimizations" dialog. Doze
         suspends background work, and a suspended VPN looks like a broken
         one: connected, but traffic stops minutes after the screen goes off.
         Asking is all we can do — the user grants it, and can revoke it. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Фоновое воспроизведение YouTube в SmartDesk. С Android 14 у медийной
         службы должно быть СВОЁ разрешение: specialUse для неё не годится, и
         система убивает такую службу при уходе с экрана. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
        android:minSdkVersion="34" />
    <!-- <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Вибрация при входящем звонке. Без этого разрешения Vibrator молча
         ничего не делает: половина «звонка» не работала ни на одном
         устройстве. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Ringing call: wakes the screen where the system grants it, and falls
         back to a heads-up banner where it does not. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- allowBackup="false": в резервную копию устройства уезжали токен
         устройства, код восстановления и ключи хранилищ — то есть доступ к
         аккаунту оказывался в облаке производителя телефона. -->
    <application
        android:name=".AngApplication"
        android:allowBackup="false"
        android:banner="@mipmap/ic_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/AppThemeDayNight.NoActionBar"
        android:usesCleartextTraffic="true">

        <!-- Portrait everywhere. The screens are laid out as one column
             sized against the viewport height, and in landscape the connect
             button was pushed off the bottom with no way to scroll to it.
             Locking is the honest fix for a UI that has no landscape design,
             rather than pretending one exists.

             Android 16+ ignores this on large screens (>600dp), so tablets
             will still rotate — the layout has to survive that on its own,
             which is what the scrolling container on the main screen is for. -->
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|screenSize|smallestScreenSize"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.server.ServerVmessActivity"
            android:exported="false"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.server.ServerVlessActivity"
            android:exported="false"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.server.ServerShadowsocksActivity"
            android:exported="false"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.server.ServerSocksActivity"
            android:exported="false"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.server.ServerHttpActivity"
            android:exported="false"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.server.ServerTrojanActivity"
            android:exported="false"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.server.ServerWireguardActivity"
            android:exported="false"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.server.ServerHysteria2Activity"
            android:exported="false"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.server.ServerCustomConfigActivity"
            android:exported="false"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.server.ServerGroupActivity"
            android:exported="false"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.server.ServerProxyChainActivity"
            android:exported="false"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.SettingsActivity"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.PerAppProxyActivity"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.AppPickerActivity"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.ScannerActivity"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.LogcatActivity"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.RoutingSettingActivity"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.RoutingEditActivity"
            android:windowSoftInputMode="stateUnchanged|adjustResize"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.SubSettingActivity"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.UserAssetActivity"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.UserAssetUrlActivity"
            android:exported="false" />

        <activity
            android:screenOrientation="portrait"
            android:name=".ui.SubEditActivity"
            android:windowSoftInputMode="stateUnchanged|adjustResize"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.ScScannerActivity"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.ScSwitchActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:process=":RunSoLibV2RayDaemon"
            android:theme="@style/AppThemeDayNight.NoActionBar.Translucent" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.ScStartActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:process=":RunSoLibV2RayDaemon"
            android:theme="@style/AppThemeDayNight.NoActionBar.Translucent" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.ScStopActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:process=":RunSoLibV2RayDaemon"
            android:theme="@style/AppThemeDayNight.NoActionBar.Translucent" />

        <activity
            android:screenOrientation="portrait"
            android:name=".ui.UrlSchemeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="v2rayng" />
                <!-- Our own scheme, so the bot's "вернуться в приложение"
                     link lands here specifically. A v2rayng:// link would
                     make Android show a chooser on any phone that also has
                     the original v2rayNG installed — and this app is built
                     to sit next to it. -->
                <data android:scheme="vpnka" />
                <data android:host="install-config" />
                <data android:host="install-sub" />
                <!-- Coming back from a card payment. The payment page is a
                     browser journey, so the only way home is a link the
                     browser can follow. -->
                <data android:host="paid" />
            </intent-filter>
        </activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.CheckUpdateActivity"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.BackupActivity"
            android:exported="false" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.AboutActivity"
            android:exported="false" />

        <service
            android:name=".service.CoreVpnService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:process=":RunSoLibV2RayDaemon"
            tools:ignore="VpnServicePolicy">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <meta-data
                android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"
                android:value="true" />
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn" />
        </service>

        <!-- Keeps the messenger socket alive off screen so calls ring. Runs in
             the DEFAULT process on purpose: it needs the same Messenger and
             CallManager singletons (and unlocked vault) as the UI, none of
             which exist in the core's own process. -->
        <service
            android:name=".service.VpnkaLinkService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:label="@string/app_name">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="messenger_call_signaling" />
        </service>

        <!-- Держит плеер YouTube, пока экран закрыт. Плеер обязан жить ЗДЕСЬ,
             а не в Compose-экране: экран умирает при сворачивании, и вместе с
             ним умирала музыка. Управление приходит с экрана блокировки и с
             наушников через MediaSession. -->
        <service
            android:name=".service.VpnkaMediaService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <service
            android:name=".service.CoreProxyOnlyService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:label="@string/app_name"
            android:process=":RunSoLibV2RayDaemon">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="proxy" />
        </service>

        <service
            android:name=".service.CoreRootService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:label="@string/app_name"
            android:process=":RunSoLibV2RayDaemon">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="proxy" />
        </service>

        <service
            android:name=".service.CoreTestService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:process=":RunSoLibV2RayDaemon">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="test" />
        </service>

        <receiver
            android:name=".receiver.WidgetProvider"
            android:exported="true"
            android:process=":RunSoLibV2RayDaemon">
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/app_widget_provider" />
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="${applicationId}.action.activity" />
            </intent-filter>
        </receiver>

        <!-- Переключение VPN с виджета. Отдельный ресивер и exported="false":
             сам WidgetProvider обязан быть открытым ради APPWIDGET_UPDATE, и
             пока переключение жило в нём, любое приложение на телефоне могло
             прислать наше действие и выключить человеку VPN. -->
        <receiver
            android:name=".receiver.WidgetToggleReceiver"
            android:exported="false"
            android:process=":RunSoLibV2RayDaemon">
            <intent-filter>
                <action android:name="${applicationId}.action.widget.click" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true"
            android:label="BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".service.QSTileService"
            android:exported="true"
            android:foregroundServiceType="specialUse"
            android:icon="@drawable/ic_stat_name"
            android:label="@string/app_tile_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:process=":RunSoLibV2RayDaemon">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="tile" />
        </service>
        <!-- =====================Tasker===================== -->
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.TaskerActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" />
            </intent-filter>
        </activity>

        <!-- exported="false": ресивер стартует и ГАСИТ VPN по внешнему
             сообщению. Оставаясь открытым, он давал это право любому
             приложению на телефоне. Автоматизация через Tasker при этом
             перестаёт работать — сознательный размен: у нас потребительский
             VPN, а не среда автоматизации. -->
        <receiver
            android:name=".receiver.TaskerReceiver"
            android:exported="false"
            android:process=":RunSoLibV2RayDaemon">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.FIRE_SETTING" />
            </intent-filter>
        </receiver>
        <!-- =====================Tasker===================== -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">

            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />

        </provider>

        <service
            android:name="androidx.work.multiprocess.RemoteWorkManagerService"
            android:exported="false"
            android:process=":bg" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.cache"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/cache_paths" />
        </provider>

    </application>

</manifest>
